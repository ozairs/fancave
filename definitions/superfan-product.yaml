product: '1.0.0'

info:
  name: superfan
  title: superfan
  version: '1.0.0'

apis:
  'superfan':
    $ref: superfan.yaml

visibility:
  view:
    type: public
  subscribe:
    type: authenticated

plans:
  default:
    title: Default Plan
    description: Default Plan
    approval: false
    rate-limit:
      value: 100/hour
      hard-limit: false
